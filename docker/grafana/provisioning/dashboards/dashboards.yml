apiVersion: 1
providers:
  - name: PowerWatch
    orgId: 1
    folder: PowerWatch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json